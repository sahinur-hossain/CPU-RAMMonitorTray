# CPU/RAM Monitor (Tkinter + psutil)

A lightweight Python desktop widget that displays real-time CPU and RAM usage.  
Built with **Tkinter** for the GUI and **psutil** for system resource monitoring.

---

## ✨ Features
- 📊 Shows live CPU and RAM usage percentages (updates every second).
- 🎨 Minimal floating widget with custom background color.
- 📌 Always-on-top window for quick monitoring.
- 🖱️ Drag the widget around the screen by holding **left mouse button**.
- ❌ Close the widget by right-clicking on it.
